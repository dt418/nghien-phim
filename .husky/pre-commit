# Kill any running pnpm dev process
if [ "$OS" = "Windows_NT" ]; then
	taskkill /F /IM "node.exe" /FI "WINDOWTITLE eq pnpm dev*" >nul 2>&1 || true
else
	pkill -f "pnpm.*dev" || true
fi

# Run build and lint-staged
pnpm run build
lint-staged
